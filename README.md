# ws101_project
A single-page application (SPA) using Vite + TypeScript + React that fetches data from a public REST API and presents it with typed components, custom hooks, and proper state management.
